请根据以下要求生成项目代码和 README。

## 项目概述

**校园技能交换** — 面向大学生的技能互助平台，学生可发布自己擅长的技能和需求，系统智能匹配互补的技能交换对象，促进校园内的知识共享与互助学习。

**项目类型：全栈Web应用**

## 项目主要需求

1. **技能发布与管理**：学生可发布自己愿意提供的技能（如摄影、编程、乐器、外语、平面设计、健身指导等），每个技能包含描述、熟练程度、可交换的时间段、期望获得的回报类型（技能交换/小额报酬/请吃饭/无偿）。支持上传技能展示作品或证书。

2. **需求发布与浏览**：学生可发布自己需要的技能帮助（如"求教 Python 数据分析"、"找人帮忙拍毕业照"），需求包含详细描述、期望时间、预算类型，其他学生可浏览需求列表并按类别/校区/时间筛选，可对感兴趣的需求进行响应。

3. **智能匹配推荐**：系统根据用户的提供技能和需求技能进行智能匹配，推荐互补的技能交换对象（如 A 会摄影想学吉他，B 会吉他想找人拍照），匹配结果以卡片形式展示，显示匹配度和共同可用时间。

4. **交换预约与确认**：匹配成功的双方可发起交换预约，选择交换时间和地点（线上/线下），预约需双方确认后生效，系统生成交换订单，预约前可在线沟通协商细节。

5. **评价与信用体系**：每次交换完成后，双方可互相评价（评分+文字评价），评价公开显示在个人主页，系统根据评价和交换次数计算用户信用分和等级，信用分影响匹配优先级和首页推荐权重。

6. **消息通知系统**：内置站内消息系统，支持用户之间的即时沟通，新消息以红点提示，支持系统通知（匹配推荐、预约提醒、评价提醒），消息记录持久化存储，支持按会话查看历史消息。

7. **个人主页与技能墙**：每个用户拥有个人主页，展示技能标签墙、历史交换记录、收到的评价、信用等级，技能墙以可视化方式展示用户的技能图谱（如雷达图展示各项技能熟练度）。

## 代码架构要求（强制执行）

- **单一职责原则**：每个功能模块必须拆分成独立的文件维护。例如：技能管理拆分为 `skill.controller.ts`、`skill.service.ts`、`skill.repository.ts`、`skill.validator.ts`、`skill.routes.ts`；匹配功能拆分为 `match.service.ts`、`match.algorithm.ts`、`match.score.calculator.ts`；消息系统拆分为 `message.controller.ts`、`message.service.ts`、`message.repository.ts`、`notification.service.ts`。
- **源码文件要多**：鼓励细粒度拆分，禁止一个文件超过 400 行。前端组件按功能拆分，每个页面拆分为多个子组件；后端按实体/功能拆分为 controller、service、repository、middleware、validator、routes、algorithm 等独立文件。
- **统一维护常量**：所有硬编码字符串、数字、枚举值必须抽取到 `constants/` 目录下的独立文件中（如 `app.constants.ts`、`error.codes.ts`、`skill.categories.ts`、`match.rules.ts`、`credit.levels.ts`），禁止在业务代码中直接写魔法数字或字符串。
- **统一维护日志**：日志配置和日志工具类统一放在 `logger/` 或 `utils/logger.ts` 中，所有模块通过同一日志实例记录，支持分级（debug/info/warn/error）。
- **统一维护异常信息**：自定义异常类统一放在 `exceptions/` 或 `errors/` 目录下（如 `BusinessException.ts`、`ValidationException.ts`、`MatchException.ts`），错误码和错误消息集中管理，禁止在业务代码中直接抛出裸字符串错误。

## 技术要求

- **英文短名（用于 Docker Compose）**：`cyskillswap`
- **前端端口**：`FRONTEND_PORT=18629`
- **后端端口**：`BACKEND_PORT=19629`
- **前端框架**：Vue 3 + TypeScript + Vite
- **UI 库**：Element Plus
- **图表库**：ECharts（雷达图展示技能）
- **后端框架**：Go + Gin
- **数据库**：MySQL 8.0
- **缓存**：Redis
- **构建工具**：Vite（前端）
- **认证方式**：JWT

## 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=cyskillswap`，确保 Compose 项目名不依赖目录名（中文目录名会被规范化为空导致 `project name must not be empty` 错误）
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

docker-compose.yml 要求：
- **不要写 `version:` 字段**（Compose V2 已废弃，会产生 `the attribute version is obsolete` 警告）
- 顶层声明 `name: cyskillswap` 作为兜底，避免目录名为中文时项目名为空
- 所有服务使用 `container_name` 命名容器，便于管理；容器名建议带 `${COMPOSE_PROJECT_NAME:-cyskillswap}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:<port>`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也建议配置 `healthcheck`，前端 `depends_on` 后端健康
- 暴露端口：前端 `18629:80`，后端按框架默认端口，数据库使用标准端口
- 所有可变配置（端口、数据库账号密码、JWT 密钥等）通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务，不依赖额外的 `-p` 参数

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME`：英文短名（小写字母/数字/`-`/`_`），如 `cyskillswap`
- 数据库连接相关：`DB_NAME` / `DB_USER` / `DB_PASSWORD` / `DB_ROOT_PASSWORD`
- 后端：`JWT_SECRET` 或等价的密钥配置
- 端口映射：`FRONTEND_PORT`、`BACKEND_PORT`、`DB_PORT`（可选，便于本地冲突时调整）

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

自检清单（生成代码后必须确认）：
1. 在 **中文目录名** 下执行 `docker compose config --quiet` 不报错
2. 启动后 `docker compose ps` 显示全部容器 healthy
3. 浏览器访问前端端口能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`（不需要 `-p`）

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 必须包含以下信息：
- **项目类型标签**：全栈Web应用
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

## docker-compose.yml 参考骨架（务必遵循）

```yaml
name: cyskillswap   # 顶层 name 字段兜底，避免中文目录名导致 project name 为空

services:
  db:
    image: mysql:8.0
    container_name: ${COMPOSE_PROJECT_NAME:-cyskillswap}-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-cyskillswap}-redis
    restart: unless-stopped
    volumes:
      - redis_data:/data

  backend:
    build: ./backend
    container_name: ${COMPOSE_PROJECT_NAME:-cyskillswap}-backend
    restart: unless-stopped
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

  frontend:
    build: ./frontend
    container_name: ${COMPOSE_PROJECT_NAME:-cyskillswap}-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-18629}:80"
    depends_on:
      - backend

volumes:
  db_data:
  redis_data:
```

`.env.example` 参考：

```dotenv
# Compose 项目名（小写字母/数字/-/_），避免中文目录名导致 project name 为空
COMPOSE_PROJECT_NAME=cyskillswap

DB_NAME=app
DB_USER=app
DB_PASSWORD=app_pwd
DB_ROOT_PASSWORD=root_pwd

JWT_SECRET=change_me_to_a_long_random_string

FRONTEND_PORT=18629
BACKEND_PORT=19629
```

README 中务必告知用户首次启动前执行 `cp .env.example .env`。

## Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配（至少包含 `node_modules/`、`dist/`、`.env`、`.DS_Store`、`*.log`、`.idea/`、`.vscode/`、`tmp/`、`*.exe`）。
